name: castaway
description: A new Flutter podcast application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.0
  webfeed: ^0.6.0
  http: ^0.12.2
  flutter_bloc: ^6.0.5
  equatable: ^1.2.3
  dartz: ^0.9.1
  just_audio: ^0.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.1.1

flutter:
  uses-material-design: true
